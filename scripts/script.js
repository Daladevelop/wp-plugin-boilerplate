jQuery(document).ready(function($) {
	// $() will work as an alias for jQuery() inside of this function
});

(function($) {
	// Place your javascript in here
	
	// This is a self executing anonymous function which will keep your scripts from using up
	// names in the global name space, and will prevent collisions with other scripts. 
	
	// $() will work as an alias for jQuery() inside of this function 
})(jQuery);
